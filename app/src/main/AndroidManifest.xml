<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dezconto.dezconto_prot">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/splashScreenTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".otherviews.CadastroActivity" />
        <activity
            android:name=".views.MainPage"
            android:noHistory="true" />
        <!--<activity android:name=".views.Cupons" />-->
        <!--<activity-->
            <!--android:name=".views.Conta"-->
            <!--android:noHistory="true" />-->
        <!--<activity-->
            <!--android:name=".nav.NavBottom"-->
            <!--android:label="@string/title_activity_nav_bottom" />-->
        <activity
            android:name=".otherclass.BottomNav"
            android:label="@string/title_activity_bottom_nav" />
    </application>

</manifest>